<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lintex9527.android.broadcastreceiverapp">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 广播接收器，静态注册 -->
        <receiver android:name=".StaticReceiver">
            <intent-filter>
                <action android:name="com.lintex9527.android.action.STATIC_BROADCAST" />
            </intent-filter>
        </receiver>

        <!-- 一个演示广播接收器限制发送者的权限的例子 -->
        <receiver
            android:name=".WifiStateReceiver"
            android:permission="android.permission.CHANGE_WIFI_STATE"
            >
            <intent-filter>
                <action android:name="com.lintex9527.android.action.MY_WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DynamicActivity" />
        <activity android:name=".StaticActivity" />
        <activity android:name=".RestrictSenderActivity"></activity>
    </application>

</manifest>